fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3
pydantic-settings==2.9.1
httpx==0.28.1
cryptography==44.0.2
python-dotenv==1.1.0
psutil==7.0.0
# El wheel de MetaTrader5 es especifico de cada version de Python y esta
# release solo publica cp311. Con otro interprete pip dira que la version
# 'no existe' cuando lo que falta es el wheel: el venv va con Python 3.11.
MetaTrader5==5.0.4874
